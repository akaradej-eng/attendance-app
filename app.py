import streamlit as st
import pandas as pd
import gspread
import json
from datetime import datetime

# 1. เชื่อมต่อ Google Sheets จากตู้เซฟ
creds_dict = json.loads(st.secrets["google_sheet"]["credentials"])
gc = gspread.service_account_from_dict(creds_dict)
sh = gc.open("ระบบเช็คชื่อนักเรียน")

# 2. เข้าถึง 2 Tabs ที่เราสร้างไว้
ws_students = sh.worksheet("Students")
ws_attendance = sh.worksheet("Attendance")

st.set_page_config(page_title="ระบบบันทึกเวลาเรียน", layout="wide", page_icon="🏫")
st.title("🏫 ระบบเช็คชื่อรายห้อง โรงเรียนบ้านเชียงวิทยา")
st.write("เลือกชั้นเรียน ตรวจสอบสถานะ และกดบันทึกข้อมูลรวดเดียวได้เลยครับ")

# 3. ดึงข้อมูลนักเรียนทั้งหมดจาก Tab 'Students'
data = ws_students.get_all_records()

if len(data) > 0:
    df_students = pd.DataFrame(data)
    
    # ดึงรายชื่อห้องทั้งหมดที่มี โดยตัดค่าที่ซ้ำกันออก
    class_list = df_students['ชั้นเรียน'].unique().tolist()
    class_list.sort()

    # 4. สร้างส่วนสำหรับเลือกข้อมูล
    col1, col2, col3 = st.columns(3)
    with col1:
        selected_class = st.selectbox("📌 เลือกชั้นเรียน", class_list)
    with col2:
        teacher_name = st.text_input("👨‍🏫 ชื่อครูที่ปรึกษา (ผู้บันทึก)")
    with col3:
        check_date = st.date_input("📅 วันที่บันทึก", datetime.today())

    st.markdown("---")
    st.subheader(f"รายชื่อนักเรียนชั้น {selected_class}")

    # 5. กรองข้อมูลเอาเฉพาะเด็กในห้องที่เลือก
    df_room = df_students[df_students['ชั้นเรียน'] == selected_class].copy()
    
    # เพิ่มคอลัมน์สถานะสำหรับให้ครูเลือก (ค่าเริ่มต้นคือ มาเรียนปกติ)
    df_room['สถานะ'] = "มาเรียนปกติ"

    # 6. สร้างตารางแบบแก้ไขได้ (Data Editor)
    edited_df = st.data_editor(
        df_room,
        column_config={
            "สถานะ": st.column_config.SelectboxColumn(
                "สถานะการมาเรียน",
                help="เลือกสถานะการมาเรียนของนักเรียน",
                options=["มาเรียนปกติ", "สาย", "ลาป่วย/ลากิจ", "ขาดเรียน"],
                required=True
            )
        },
        # ล็อกคอลัมน์เหล่านี้ไว้ ป้องกันไม่ให้เผลอไปแก้รหัสหรือชื่อเด็กบนหน้าเว็บ
        disabled=["รหัสนักเรียน", "ชื่อ", "ชั้นเรียน", "เลขที่"], 
        hide_index=True,
        use_container_width=True
    )

    st.markdown("---")
    
    # 7. ปุ่มบันทึกข้อมูล
    if st.button("💾 บันทึกข้อมูลทั้งห้อง", type="primary"):
        if teacher_name.strip() == "":
            st.error("กรุณากรอกชื่อครูผู้บันทึกก่อนครับ ❌")
        else:
            try:
                date_str = check_date.strftime("%d/%m/%Y")
                records_to_insert = []
                
                # วนลูปนำข้อมูลจากตารางที่แก้ไขแล้ว มาเรียงเพื่อเตรียมส่งเข้า Google Sheets
                for index, row in edited_df.iterrows():
                    records_to_insert.append([
                        date_str, 
                        str(row.get('รหัสนักเรียน', '')), 
                        str(row.get('ชื่อ', '')), 
                        str(row.get('ชั้นเรียน', '')), 
                        str(row['สถานะ']), 
                        teacher_name
                    ])
                
                # ส่งข้อมูลต่อท้ายใน Tab 'Attendance' รวดเดียวเลย
                ws_attendance.append_rows(records_to_insert)
                st.success(f"บันทึกข้อมูลของห้อง {selected_class} จำนวน {len(records_to_insert)} รายการ เรียบร้อยแล้ว! ✅")
            except Exception as e:
                st.error(f"เกิดข้อผิดพลาดในการบันทึกข้อมูล: {e}")
else:
    st.info("ยังไม่มีข้อมูลนักเรียนในระบบครับ โปรดเพิ่มรายชื่อลงใน Tab 'Students' ของ Google Sheets ก่อน")
